# Copiá este archivo a apps/web/.env para desarrollo local.
# Apunta el front a la API local (ver entorno de desarrollo en el README).
# En producción esta variable se setea en Vercel (apuntando a la API de Render).
EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1
